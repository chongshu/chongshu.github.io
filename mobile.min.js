var isMobile = /iPhone|Android/i.test(navigator.userAgent);
mobile=''
if (/iPhone/i.test(navigator.userAgent)){mobile = ': iPhone'}
if (/Android/i.test(navigator.userAgent)){mobile = ': Android'}
var head = document.getElementsByTagName('HEAD')[0];
var link = document.createElement('link');
if (isMobile) {
    if (document.title =='Chong Shu') {
        if (sessionStorage.getItem('alerted') != 'yes') {
           alert('For user experience of mobile' + mobile + ' users, I show my website in landscape view. ')
           sessionStorage.setItem('alerted', 'yes')
        }
    }
    link.rel = 'stylesheet';
    link.type = 'text/css';
    link.href = 'mobile.css';
    head.appendChild(link);
} else {
    link.rel = 'stylesheet';
    link.type = 'text/css';
    link.href = 'desktop.css';
    head.appendChild(link);
}

















